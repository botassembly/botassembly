---
timeout: 120
---

Read the JSON object in $INPUT. It has an `id` and an `input` holding one note
under `text`. Write to $OUTPUT exactly one line: the note condensed to a single
sentence of at most fifteen words. Write nothing else.
