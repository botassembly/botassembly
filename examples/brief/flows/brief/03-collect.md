---
timeout: 300
---

$INPUT holds one file per note, named after the note's id. Read every one of
them and write to $OUTPUT a markdown bullet list, one bullet per file, in
filename order, each bullet the file's sentence.
