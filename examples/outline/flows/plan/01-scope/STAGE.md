---
timeout: 300
---

Read the rough topic in $INPUT and decide what sections the report outline needs.

Write to $OUTPUT the shape the schema gives you: the frontmatter keys it names,
then its two sections. `title` is the outline's title, `sections` is how many
level-three headings you wrote, `themes` lists the recurring subjects in one or
two words each, and `questions` is true when the topic leaves something
unanswered.

Write headings in the style at $SKILLS/outline-style/SKILL.md.
