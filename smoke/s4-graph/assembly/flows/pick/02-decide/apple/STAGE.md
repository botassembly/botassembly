---
---

Write exactly two lines to `$OUTPUT`:

- the first line is the word `apple`
- the second line is the line of `$INPUT/prepare.txt` that begins
  `PRIMED-MARKER-`, copied exactly

Write nothing else.
