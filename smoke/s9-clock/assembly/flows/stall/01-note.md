---
---

`$INPUT/request.txt` holds one line, and that line is a token.

Write that token to `$OUTPUT` exactly as it appears — the same characters, in
the same order. Write nothing else: no quotes, no explanation, no heading.
