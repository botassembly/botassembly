---
---

`$INPUT/request.txt` holds one line, and that line is a single token.

Write that token to `$OUTPUT` exactly as it appears — the same characters, in
the same order. Write nothing else: no quotes, no explanation, no heading.

There is no second token in that file, so checklist item 2 cannot be done: mark
item 2 skipped, with that as the reason.

Mark each checklist item below with the `mark` tool as you settle it. An item
left unmarked blocks the stage.

## Checklist

- Read `$INPUT/request.txt` and take the token from it
- Take the second token from `$INPUT/request.txt`
- Write the token to `$OUTPUT`
