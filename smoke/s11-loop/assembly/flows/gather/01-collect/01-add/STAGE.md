---
---

`$INPUT/request.txt` holds one line, and that line is a token.

If `$INPUT/add.txt` exists, copy every line of it to `$OUTPUT` first. Then add
exactly one more line to `$OUTPUT`, holding that token and nothing else.

Add exactly one line each time you are asked, however many lines you copied.
