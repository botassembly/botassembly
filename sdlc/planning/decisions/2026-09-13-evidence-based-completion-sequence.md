# Evidence-based completion sequence

Decided 2026-09-13. The first implementation pass and two independent repository surveys replaced eleven forecast outcomes with nine concrete implementation outcomes, including ticket 0274. Astra extra-eyes accepted the consolidation. A ticket count is not a target. New independent evidence may split, merge, add, or remove work.

## Consolidation

Recursive token totals, exact timestamps, and impossible-duration rejection form one truthful run-summary contract. Descent admission and complete static assembly-check output form one assembly-check accuracy contract. Clean-home live authentication and WSL qualification belong to the exact release candidate instead of standalone implementation tickets. A failed qualification creates implementation work; release does not waive it.

The request-ingestion outcome covers bounded piped input and seekable task files plus black-box pipeline settlement. Existing raw request, record, and result delivery already has bounded streaming and early-reader-close tests. It stays unless a failing test proves a gap.

Platform work creates permanent Linux and macOS checks, exercises installation outside the current gate, retains cleanup observations as test obligations, and gives native Windows clear WSL guidance. The documentation deployment dependency must follow the complete check and later expand with that platform gate.

Model diagnostics may report a local catalog miss. They must not invent provider retirement or global unavailability. The public documentation pass corrects current claims and stale references after runtime behavior settles. The existing biomedical vocabulary guard and historical changelog and record provenance remain.

## Rejected work

The sequence does not reopen access restrictions, executable allowlists, chooser redesign, minimal environments, custom secret scanners, native Windows support, speculative provider timeouts, unreproduced cleanup repairs, Pi changes, a duplicate biomedical scrub, or a rewrite of working raw-output streaming.

Ian can overturn the ordering and grouping. The trusted-execution, external-containment, Pi-reference-only, platform, and final-publication boundaries remain his accepted rulings.
