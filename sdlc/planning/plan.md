# Bot specification and runtime plan

Updated 2026-09-11. This is the current planning entry point. The specification states today's contract. Tickets describe changes to it. The ideal-state summary describes the destination.

## Direction

Build a readable file format and a dependable runtime. Prioritize starting, checking, stopping, resuming, and inspecting a run. Keep control mechanisms tied to actual procedures. The early 0201 comparison is complete. Later model experiments follow the known repairs.

The [direction decision](decisions/2026-09-08-ideal-state-direction.md) records the scope and trade-offs. The [planning alignment decision](decisions/2026-09-08-planning-alignment.md) records today's ordering and ticket corrections.

The [0201 model comparison decision](decisions/2026-09-08-model-comparison-0201.md) records the Luna High and Sol Medium experiment, its scoring, retry rules, and landing procedure.

The [complexity routing follow-up](decisions/2026-09-09-complexity-routing-follow-up.md) applies the reviewed rubric to the completed contraction work. The [post-contraction strategy](decisions/2026-09-09-post-contraction-strategy.md) records Astra's extra-high oracle review, the next ticket set, and the costs accepted.

The [Pi and command ticket sequence](decisions/2026-09-11-pi-and-command-ticket-sequence.md) records the reviewed ticket splits, compatibility rules, trust boundaries, model routes, and shared verification plan. The [legacy command disposition](decisions/2026-09-11-legacy-command-disposition.md) preserves complete-record and session reading and retires the other unsupported flat operations.

## Next work

Manual tickets 0084 and 0085 prepared the `0.0.1` public-alpha source tree and reduced the repository to one branch and one worktree. The first hosted checks on the new root commit exposed two release-path faults. Tickets 0232 and 0233 repaired both. Tickets 0234 and 0235 repaired the two reproduced local test races before runtime feature work.

This is the accepted sequence for the remaining repair work. Each ticket receives independent design review, red-green implementation, and independent code review. Closely related tickets may share one complete integration check after their focused proofs pass. No ticket completes before its integrated local and hosted checks pass. Recheck a draft's premise after each preceding landing. Priorities order ready work; they do not override a hold or a dependency.

Ticket 0232 completed the root-safe runtime workflow at `b7e2d22`. Ticket 0233 kept the documentation build required and made Pages deployment opt-in at `f526e49`. Ticket 0234 moved documentation generator tests into private fixtures at `5f749be`. Ticket 0235 made both escaped-process tests publish safe cleanup PIDs through captured output at `12927bb`. Ticket 0248 moved the test toolchain to the patched Vitest 4.1.11 family at `e317ae7`. Ticket 0236 enforced a self-contained maintained-prose boundary at `dc02559`. Ticket 0237 added the supported checklist-mark reader at `2e70525`. Ticket 0238 made piped request input wait for clean end-of-file at `4088a7f`. Ticket 0239 gave local dot installs and links useful names at `b32d506`. Ticket 0240 added a direct diagnostic for misplaced home options at `707cd89`. Ticket 0241 stated the existing non-sandbox and file-observation boundary at `4656937`. Ticket 0242 adopted Pi's public model-runtime boundary at `8659844`. Ticket 0243 put Pi behind Bot-owned runtime types at `0af904c`. Tickets 0250 through 0252 now decode, page, and index Pi format-4 transactions without losing logical entries. The reader batch passed 88 repository tests, 1,582 runtime tests, 143 conformance cases, and the complete static gate on current main. Ticket 0249 moved all three Pi packages to 0.85.1 at `c82ff00`. Ticket 0244 made Pi's ModelRuntime the provider and model source at `a12249c`. Ticket 0245 made Pi's supported authentication store the shared credential source at `97a3e38`. Tickets 0246, 0253 through 0260 moved the current assembly, model, and authentication commands onto explicit contracts at `7a65c7d`. The integrated command batch passed 234 test files, 1,776 runtime tests, 143 conformance cases, the complete static gate, and both hosted workflows. Ticket 0247 migrated maintained callers at `a13c663`. Tickets 0261 and 0262 added current complete-record and session readers at `0e6b9f6`. Their shared local and hosted checks passed. Ticket 0217 deleted the legacy command surface at `a0a44df`. Its local and hosted checks passed. No draft remains ready for implementation.

The numbered list sets work order. Ticket dependencies name only technical prerequisites. Archived ticket 0231 preserves the original Bot and Pi mismatch measurements. Tickets 0242 through 0245 and 0249 through 0252 preserve the decisions and implementation evidence that closed it.

The contraction through manual ticket 0072 is complete. Its records preserve the landed details. Manual tickets 0073 and 0074 gave the assembly-update and hostile-provider tests explicit timing and cleanup boundaries. Manual ticket 0075 isolated concurrent coverage output through verification and atomic publication. Manual ticket 0076 moved the read-only runtime workflow to reviewed Node 24 action releases. Manual tickets 0077 through 0079 narrowed documentation permissions, pinned its direct actions, and added one complete local workflow policy. Manual ticket 0080 aligned the published pre-release and resume contracts with implemented behavior. Manual ticket 0081 proved model selection across every invocation path without changing production behavior. Manual ticket 0082 added the supported bounded one-run reading. Manual ticket 0083 migrated Bot smoke to that reading. Manual ticket 0084 prepared the publishable `0.0.1` source tree. Drafts 0229, 0225, 0227, 0228, and 0230 are complete.

## Model routing

Sol Medium won the level-3 0201 comparison noticeably. Use the reviewed complexity rubric before assigning each new implementation. The source ticket records risk facts. The design author adds the five-factor score. Sol Medium designs and reviews every ticket. Design review scored 0227 at level 2, so Luna High implemented it. Design review scored completed draft 0228 at level 3, so Sol Medium implemented it. Current evidence points to Luna High for 0230. The privileged documentation workflow in completed draft 0229 had a level-4 security floor and stayed with Sol Medium. The design score can change an assignment. The [comparison decision](decisions/2026-09-08-model-comparison-0201.md), [routing follow-up](decisions/2026-09-09-complexity-routing-follow-up.md), and [post-contraction strategy](decisions/2026-09-09-post-contraction-strategy.md) record the evidence and costs.

## Work that waits

| Work | What releases it |
| --- | --- |
| 0208: expose attempts, retries, and send-backs | A caller needs those metrics from the supported one-run reading. Separate the one-based attempt ordinal, additional attempts, actual send-backs, provider retries, blockers, exhaustion, machinery failure, and incomplete work. |
| 0136: identify and bound a provider stall | Completed-operation evidence and independently identified stalls support a safe liveness rule. An open websocket beside a stall does not prove causation. |
| 0220: contract installation storage | The project accepts a narrower guarantee set and identifies a concrete simplification worth the lost protection. |
| Publish the Pages site | Done 2026-09-11. Ian enabled GitHub Pages with the Actions source, set `PUBLISH_PAGES` to true, and run `34592962118` deployed. The custom domain botassembly.org moved from the stale `botassembly.github.io` repository to this one; that repository is now unused and Ian can archive it. Hover holds the DNS: apex A records to GitHub Pages, www CNAME to botassembly.github.io. |

Manual ticket 0057 completed first-use identity creation and removed `bot home init`. Manual ticket 0058 completed provider-cause reporting from draft 0200. Manual ticket 0059 completed the recorded-check reader from draft 0201. Manual ticket 0060 completed the home-busy reader from draft 0218. Manual ticket 0061 completed the end-time and duration reading from draft 0207. Manual ticket 0062 completed the source identity reading from draft 0209. Manual ticket 0063 completed the lifecycle provenance repair for checkout umask. The permission issue is complete. Manual ticket 0064 completed draft 0181 by making provider-retry tests wait for published records under a bounded deadline. Manual ticket 0065 completed draft 0203 by moving the derived search index out of the home without changing status or prune. Manual ticket 0066 completed draft 0204 by omitting source dot-entries from copied install and update. Manual ticket 0067 completed draft 0205 by removing the case-colliding fixture path and adding a tracked-path lint check. Manual ticket 0069 completed draft 0221 by moving the live smoke inspection onto current command contracts and making token-accounting failures stop their rung. Manual ticket 0068 completed draft 0206 by proving normalized smoke roots through both required live path forms. Manual ticket 0070 completed draft 0216 by removing planning-prose tests and enforcing that boundary. Manual ticket 0071 completed draft 0215 by folding three one-importer helpers into their owners. Manual ticket 0072 completed draft 0219 by simplifying the trusted source snapshot while preserving digest meaning. Manual ticket 0073 completed draft 0222 by controlling the assembly-update interruption boundary and settling every child path. Manual ticket 0074 completed draft 0223 by giving hostile-provider tests explicit lifecycle and cleanup ownership. Manual ticket 0075 completed draft 0224 by giving each coverage invocation private output through verification and atomic publication. Manual ticket 0076 completed draft 0226 by updating the read-only runtime workflow to reviewed Node 24 action releases. A separate held draft will carry any future storage contraction after the specification names the exact retained guarantees.

Drafts stay unpromoted until the manual workstream selects one. Source draft numbers and manual ticket numbers are separate namespaces. Copy one source draft into the manual workstream, resolve any dependency against completed manual records, and keep the source reference in the design. No dispatcher owns this process.

## Gaps after the repairs

- Compare parent-only work with bounded delegation on the same requests. Retain inputs, raw evidence, resolved model choices, checks, and total parent-and-child consumption. Include review, failed children, and repairs. Start with read-only evidence gathering and a bounded consultation.
- Evaluate answer quality separately from runtime conformance. Unknown usage stays unknown. A model mapping is reversible configuration; a cheaper mapping must preserve required reviews.
- Have an unfamiliar author follow the guides through creating, moving, running, and inspecting an assembly after command names settle. No outside reader is scheduled by this plan.
- Recheck the unresolved older questions and rough-edge reports preserved in [the planning archive](archive/2026-09-08-open-questions.md) and [the old punchlist](archive/2026-09-08-punchlist.md). Treat each as a hypothesis. Reproduce it or retire it before it becomes a ticket.
- Keep the [clone-install cleanup finding](../issues/2026-08-21-install-clone-cleanup-flake.md) open until its cause is explained or its recorded observation window closes. A focused pass alone does not explain an intermittent failure.
- Keep the [subflow cleanup finding](../issues/2026-09-08-subflow-local-signal-cleanup-enotempty.md) open until another occurrence, a proved cause, or 2026-10-08. Promote it on recurrence or cause. Close it at the date if it stays absent.
- Work the five website and examples findings filed on 2026-09-11: the [retired mark](../issues/2026-09-11-retired-mark-and-duplicated-title.md) first, then [theming](../issues/2026-09-11-docs-site-is-unthemed-starlight.md), the [home-page pitch](../issues/2026-09-11-home-page-does-not-pitch-the-project.md), the [documentation assessment](../issues/2026-09-11-documentation-has-never-been-assessed.md), and the [examples program](../issues/2026-09-11-no-public-examples-program.md). The examples program decomposes into one ticket per example.
- Define an explicit way to inspect and remove orphaned external find-cache entries after real operator use shows the needed boundary. Status still counts only home bytes. Prune still owns run and scratch cleanup only.
- Audit current executable and recorded operator use before filing `run session`, `run tools`, `home status`, or `home prune`. Tests and examples define behavior. They do not prove demand.

## Verification boundary

The [post-contraction strategy](decisions/2026-09-09-post-contraction-strategy.md) records the current oracle evidence and limitations. Read current records and machine state for completion status. Manual ticket 0085 records the completed ref and worktree cleanup plus its private recovery evidence.

## Questions and constraints

No new product decision blocks the ordered draft above. Ian can defer it. Future features need an observed use case. Whole-run deadlines, cost budgets, and a home-level routing agent remain uncommitted possibilities. This plan adds none. Ian published the Pages site on 2026-09-11; the docs workflow now deploys on every push to main.
